#ifndef H_basemath //START basemath.hpp
#define H_basemath

#include <cmath>
typedef float real;

#define MAX(a,b) ((a)>=(b) ? (a) : (b))
#define MIN(a,b) ((a)<=(b) ? (a) : (b))
#define ABS(x) ((x)>0 ? (x) : -(x))
#define SQ(x) ((x)*(x))

#endif // END basemath.hpp

